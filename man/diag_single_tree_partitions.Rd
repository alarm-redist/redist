% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diag_single_tree_partitions.R
\name{diag_single_tree_partitions}
\alias{diag_single_tree_partitions}
\title{Diagnose Single-Tree MMSS Partitions}
\usage{
diag_single_tree_partitions(map, l, n_plans = 50, n_trees_per_region = 200)
}
\arguments{
\item{map}{A \link{redist_map} object.}

\item{l}{Number of districts to merge into each sampled region \code{R}.}

\item{n_plans}{Number of plans to draw with \code{\link[=redist_smc]{redist_smc()}}. Defaults to \code{50}.}

\item{n_trees_per_region}{Number of spanning trees to draw for each sampled
merged region. Defaults to \code{200}.}
}
\value{
A list with elements:
\itemize{
\item \code{hit_rate}: \code{Pr[m(T) >= 1]} across all sampled \verb{(R, T)} pairs.
\item \code{mean_m}: \code{E[m(T)]}.
\item \code{max_m}: the maximum observed value of \code{m(T)}.
\item \code{dist_m}: the empirical distribution of \code{m(T)} as a named integer vector.
\item \code{cv2_Z}: \code{var(Z) / mean(Z)^2} for the sampled reference-plan reverse
proposal weight.
}
}
\description{
Samples plans from \code{map}, chooses one merged region \code{R} per sampled plan by
selecting \code{l} adjacent districts, then draws spanning trees on each induced
subgraph \code{G[R]} and counts the valid \code{l}-partitions of each tree.
}
\details{
For each sampled plan, the diagnostic:
\itemize{
\item draws one plan with \code{\link[=redist_smc]{redist_smc()}}
\item selects \code{l} connected districts using the same random expansion rule used by
MMSS
\item forms the merged region \code{R} from those districts
\item draws \code{n_trees_per_region} uniform spanning trees on \code{G[R]}
\item counts the number \code{m(T)} of valid \code{l}-partitions of each tree
\item records the reverse-proposal weight contribution
\verb{Z = 1[xi_ref in P(T)] / m(T)}
}

A valid \code{l}-partition is a set of \code{l - 1} tree edges whose removal gives \code{l}
connected components, each with population inside
\verb{[(1 - pop_tol) * pop(R) / l, (1 + pop_tol) * pop(R) / l]}.
}
