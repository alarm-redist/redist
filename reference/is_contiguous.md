# Check that a `redist_map` object is contiguous

Check that a `redist_map` object is contiguous

## Usage

``` r
is_contiguous(x)
```

## Arguments

- x:

  the object

## Value

`TRUE` if contiguous.
