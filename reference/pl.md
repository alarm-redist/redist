# Access the Current `redist_plans()` Object

Useful inside piped expressions and `dplyr` functions.

## Usage

``` r
pl()
```

## Value

A `redist_plans` object, or `NULL` if not called from inside a `dplyr`
function.

## Examples

``` r
pl()
#> NULL
```
