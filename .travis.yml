# Sample .travis.yml for R projects

language: 
    - r
    - cpp

cache: packages
sudo: required

compiler:
    - clang
    - gcc
    - g++

warnings_are_errors: false

env:
  global: 
    - CRAN: http://cran.rstudio.com

notifications:
  email:
    on_success: change
    on_failure: change
