# Sample .travis.yml for R projects

language: r
cache: packages
sudo: required

warnings_are_errors: false

env:
  global: 
    - CRAN: http://cran.rstudio.com

notifications:
  email:
    on_success: change
    on_failure: change
